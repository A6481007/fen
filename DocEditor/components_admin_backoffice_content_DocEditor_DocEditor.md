"use client";

// Thin re-export to keep legacy imports working while the main implementation lives in index.tsx.
export { DocEditor as default, DocEditor } from "./index";
export type { DocEditorProps } from "./index";
