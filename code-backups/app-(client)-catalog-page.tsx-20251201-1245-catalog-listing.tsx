import ProductPage from "../product/page";

// Temporary alias so /catalog serves the product catalog UI until dedicated catalog experience exists.
const CatalogPage = () => <ProductPage />;

export default CatalogPage;
